apiVersion: v1
kind: Service

metadata:
  name: {{SERVICE_NAME}}

spec:
  type: NodePort

  selector:
    app: {{SERVICE_NAME}}

  ports:
    - port: 80
      targetPort: {{CONTAINER_PORT}}
      nodePort: {{NODE_PORT}}
