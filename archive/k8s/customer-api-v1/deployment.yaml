apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-api-v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-api-v1
  template:
    metadata:
      labels:
        app: customer-api-v1
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: customer-api-v1
          image: ghcr.io/marcel-platform/customer-api-v1:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8081
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 5
