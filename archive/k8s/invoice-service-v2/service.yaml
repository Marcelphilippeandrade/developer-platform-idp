apiVersion: v1
kind: Service

metadata:
  name: invoice-service-v2

spec:
  type: NodePort

  selector:
    app: invoice-service-v2

  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
