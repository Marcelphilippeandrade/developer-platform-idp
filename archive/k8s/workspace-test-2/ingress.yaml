apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: workspace-test-2-ingress

spec:
  rules:
    - host: workspace-test-2.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: workspace-test-2
                port:
                  number: 80
