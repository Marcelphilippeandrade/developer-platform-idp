apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: teste-service-ingress

spec:
  rules:
    - host: teste-service.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: teste-service
                port:
                  number: 80
