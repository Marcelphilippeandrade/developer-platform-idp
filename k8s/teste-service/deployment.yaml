apiVersion: apps/v1
kind: Deployment
metadata:
  name: teste-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: teste-service
  template:
    metadata:
      labels:
        app: teste-service
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: teste-service
          image: ghcr.io/marcelphilippeandrade/teste-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
