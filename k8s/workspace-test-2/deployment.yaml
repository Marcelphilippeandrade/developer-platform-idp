apiVersion: apps/v1
kind: Deployment
metadata:
  name: workspace-test-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workspace-test-2
  template:
    metadata:
      labels:
        app: workspace-test-2
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: workspace-test-2
          image: ghcr.io/marcelphilippeandrade/workspace-test-2
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
