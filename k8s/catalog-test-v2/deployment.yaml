apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-test-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-test-v2
  template:
    metadata:
      labels:
        app: catalog-test-v2
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: catalog-test-v2
          image: ghcr.io/marcelphilippeandrade/catalog-test-v2
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
